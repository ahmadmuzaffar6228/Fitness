android {
            defaultConfig {
                applicationId = "org.nativescript.Fitness"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }